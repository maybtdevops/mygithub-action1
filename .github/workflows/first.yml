
name: myfirstjob
on: push
jobs:
  first-job:
    runs-on: ubuntu:latest

    steps:
      - name : welcome message 
        run: echo "Hello World"
      - name: "list"
        run: ls -l 

